<%= simple_form_for @appointment, method: :patch do |f| %>
  <%= f.input :recommendation, as: :text,
              wrapper_html: { class: "d-flex flex-column mb-2" },
              input_html: { class: "w-25 mx-auto", height: "4" } %>
  <%= f.button :submit, "Submit", class: "btn btn-primary nav-btn" %>
<% end %>
