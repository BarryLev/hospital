<%= simple_form_for @user, method: :patch, url: user_path do |f| %>
  <%= f.input :avatar, as: :file, input_html: { accept: ".jpg, .png" } %>
  <%= f.button :submit, "Submit", class: "btn btn-primary nav-btn" %>
<% end %>
